# 软键盘连续输入后按键音消失：排查与修改方案

## 1. 问题现象

操作过程：

```text
1. 启动 sonoquest.exe
2. 打开软键盘
3. 点击字符，例如 A、B、C
4. 开始输入时存在按键音
5. 快速连续输入多个字符后，按键音消失
6. 字符仍然可以正常输入
```

第 6 点非常重要。

字符仍然能够输入，说明：

- 软键盘程序没有完全停止工作。
- 主程序仍然能够处理字符输入。
- 输入框本身没有失效。
- 问题更可能出现在按键音播放模块。

排查时不要一开始就修改代码。应该先确认问题在哪一段链路中发生。

---

## 2. 先做对照实验

分别执行两组测试。

### 2.1 慢速输入

每输入一个字符，间隔大约 1 秒。

观察按键音是否一直正常。

### 2.2 快速输入

快速连续点击多个字符。

观察按键音是否在输入几个字符后消失。

如果慢速输入正常，快速输入容易失声，说明问题很可能和“上一个声音还没有播放结束，下一个声音又开始播放”有关。

项目中的按键音文件大约持续 `135 ms`。如果两次按键的间隔小于 135 ms，第二次播放请求到达时，第一次声音可能仍处于播放状态。

---

## 3. 认识软键盘和主程序

软键盘是一个独立的可执行程序：

```text
D:\workspace\sonoquest_oem\sq_common\bin\x64\OnScreenKeyboard.exe
```

主程序是：

```text
sonoquest.exe
```

它们不是同一个进程。软键盘点击按键以后，需要通过 Windows 消息把按键信息发送给 `sonoquest.exe`。

可以把整体过程理解为：

```text
用户点击软键盘
    -> OnScreenKeyboard.exe 发送 Windows 消息
    -> sonoquest.exe 接收消息
    -> sonoquest.exe 处理按键
    -> sonoquest.exe 播放按键音
```

当前仓库中只有 `OnScreenKeyboard.exe`，没有软键盘对应的完整源码。因此应该先排查主程序接收消息和播放声音的代码。

---

## 4. 从软键盘消息入口开始看

打开文件：

```text
D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp
```

找到函数：

```cpp
BOOL CSonoquestDlg::OnCopyData(
    CWnd* pWnd,
    COPYDATASTRUCT* pCopyDataStruct)
```

当前分支中，该函数大约位于：

```text
sonoquestDlg.cpp 第 4785 行
```

`OnCopyData()` 是主程序接收其他进程 `WM_COPYDATA` 消息的入口。

继续在这个函数中找到：

```cpp
else if (pCopyDataStruct->dwData == 2) // Redirected key down
{
    MSG msg;
    msg.message = WM_KEYDOWN;
    msg.lParam = 0;
    msg.wParam = *((WPARAM*)(pCopyDataStruct->lpData));
    PreTranslateMessage(&msg);
}
```

当前分支中，这段代码大约位于：

```text
sonoquestDlg.cpp 第 5124 行
```

它的含义是：

```text
dwData == 2
    -> 软键盘发送的是“按键按下”消息
    -> 主程序构造一个 WM_KEYDOWN 消息
    -> 调用 PreTranslateMessage() 处理这个按键
```

这里的 `msg.wParam` 保存具体按下的是哪个键。

---

## 5. 找到按键音调用

仍然打开：

```text
D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp
```

找到：

```cpp
BOOL CSonoquestDlg::PreTranslateMessage(MSG* pMsg)
```

当前分支中，该函数大约位于：

```text
sonoquestDlg.cpp 第 1801 行
```

函数开头会读取触控按键音量：

```cpp
sysconfig& mysysconfig = *sysconfig::getInstance();
int vol = mysysconfig.GetTouchVolume() * 10;
```

随后拼接按键音文件路径：

```cpp
ostringstream oss;
oss << st_dir.get_app_dir()
    << "res\\beep"
    << vol
    << ".wav";

string beep = oss.str();
```

例如触控音量为 `3`，最终使用的文件类似：

```text
程序运行目录\res\beep30.wav
```

在 `WM_KEYDOWN` 分支中可以看到：

```cpp
myaudio.play(beep.c_str());
```

当前分支中，这一行大约位于：

```text
sonoquestDlg.cpp 第 1861 行
```

到这里可以整理出完整调用链：

```text
OnScreenKeyboard.exe
    -> CSonoquestDlg::OnCopyData()
    -> 构造 WM_KEYDOWN
    -> CSonoquestDlg::PreTranslateMessage()
    -> myaudio.play()
    -> audio::play()
```

---

## 6. 找到真正的声音播放代码

打开：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

找到：

```cpp
bool audio::play(const char* fname)
```

当前代码为：

```cpp
bool audio::play(const char* fname)
{
    alSourcei(source, AL_BUFFER, 0);

    if (load(fname))
    {
        alSourcei(source, AL_BUFFER, buffer);
        alSourcePlay(source);
        return 1;
    }

    return 0;
}
```

这个文件使用 OpenAL 播放声音。

可以先简单理解几个概念：

```text
device   音频设备，可以暂时理解为声卡
context  OpenAL 的运行环境
buffer   保存 WAV 文件的声音数据
source   播放 buffer 的播放器
```

一次正常播放应该是：

```text
读取 WAV 文件
    -> 将声音数据放入 buffer
    -> 将 buffer 绑定到 source
    -> 调用 alSourcePlay(source)
```

---

## 7. 当前代码为什么可疑

当前 `audio::play()` 一开始就执行：

```cpp
alSourcei(source, AL_BUFFER, 0);
```

这行代码表示将当前 buffer 从 source 上解绑。

但是代码没有先停止正在播放的 source。

快速连续输入时，可能出现：

```text
第一个按键
    -> source 开始播放，声音长度约 135 ms

第二个按键很快到来
    -> 第一个声音仍处于 AL_PLAYING 状态
    -> 代码直接尝试解绑 AL_BUFFER
    -> OpenAL 可能返回 AL_INVALID_OPERATION
```

OpenAL 的错误值：

```text
AL_INVALID_OPERATION = 0xA004
```

原代码没有认真处理这些错误，因此可能出现：

```text
消息仍然正常
字符仍然正常输入
myaudio.play() 仍然被调用
但 OpenAL 内部操作已经失败
最终听不到后续按键音
```

正确顺序应当是：

```text
停止上一次声音
    -> 解绑旧 buffer
    -> 加载 WAV 文件
    -> 绑定新 buffer
    -> 播放新声音
```

---

## 8. 修改之前先用断点确认

不要只根据猜测直接修改。先使用 Visual Studio 断点确认调用链。

### 8.1 设置断点

在 Visual Studio 中，点击代码行左侧空白区域，或者按 `F9` 设置断点。

建议设置三个断点。

#### 断点 1：软键盘消息入口

文件：

```text
D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp
```

位置：

```cpp
else if (pCopyDataStruct->dwData == 2)
```

大约在第 5124 行。

#### 断点 2：调用按键音

文件：

```text
D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp
```

位置：

```cpp
myaudio.play(beep.c_str());
```

大约在第 1861 行。

#### 断点 3：真正播放声音

文件：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

位置：

```cpp
bool audio::play(const char* fname)
```

大约在第 121 行。

### 8.2 Visual Studio 常用调试快捷键

```text
F5   启动调试或继续运行
F9   添加或删除断点
F10  单步执行，但不进入调用的函数
F11  单步执行，并进入调用的函数
Shift+F11 跳出当前函数
```

### 8.3 调试过程

```text
1. 选择 Debug 配置
2. 选择 x64 平台
3. 按 F5 启动程序
4. 打开软键盘
5. 输入字符
6. 程序在断点处暂停后，按 F5 继续
7. 一直输入到按键音消失
8. 按键音消失后，再输入一个字符
9. 观察三个断点是否仍然进入
```

### 8.4 如何判断结果

#### 情况 A：断点 1 不再进入

说明软键盘没有继续向主程序发送按键消息。

排查方向应转向：

```text
OnScreenKeyboard.exe
WM_COPYDATA 消息发送
软键盘进程状态
```

#### 情况 B：断点 1 进入，但断点 2 不进入

说明主程序收到消息，但消息没有正常进入按键处理流程。

需要检查：

```cpp
pCopyDataStruct->dwData
msg.message
msg.wParam
```

#### 情况 C：三个断点都正常进入，但没有声音

说明：

```text
软键盘消息正常
主程序按键处理正常
myaudio.play() 正常调用
问题位于 st_audio.cpp 或 OpenAL
```

当前代码最可能属于情况 C。

---

## 9. 第一处修改：修正 audio::play()

打开：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

找到原函数：

```cpp
bool audio::play(const char* fname)
{
    alSourcei(source, AL_BUFFER, 0);
    if (load(fname))
    {
        alSourcei(source, AL_BUFFER, buffer);
        alSourcePlay(source);
        return 1;
    }
    return 0;
}
```

将整个函数替换为：

```cpp
bool audio::play(const char* fname)
{
    if (fname == NULL || fname[0] == '\0')
    {
        return false;
    }

    // 清除之前遗留的 OpenAL 错误。
    while (alGetError() != AL_NO_ERROR)
    {
    }

    // 快速连续输入时，上一次按键音可能还没有播放结束。
    // 必须先停止 source，才能安全更换它绑定的 buffer。
    alSourceStop(source);

    ALenum error = alGetError();
    if (error != AL_NO_ERROR)
    {
        TRACE(
            "audio::play: alSourceStop failed, error=0x%X\n",
            error);
        return false;
    }

    // source 已停止，现在解绑旧 buffer。
    alSourcei(source, AL_BUFFER, 0);

    error = alGetError();
    if (error != AL_NO_ERROR)
    {
        TRACE(
            "audio::play: detach buffer failed, error=0x%X\n",
            error);
        return false;
    }

    // 将指定 WAV 文件加载到已有的 OpenAL buffer。
    if (!load(fname))
    {
        TRACE(
            "audio::play: load failed, file=%s\n",
            fname);
        return false;
    }

    // 把已经加载声音数据的 buffer 绑定到 source。
    alSourcei(source, AL_BUFFER, buffer);

    error = alGetError();
    if (error != AL_NO_ERROR)
    {
        TRACE(
            "audio::play: attach buffer failed, error=0x%X\n",
            error);
        return false;
    }

    // 播放本次按键音。
    alSourcePlay(source);

    error = alGetError();
    if (error != AL_NO_ERROR)
    {
        TRACE(
            "audio::play: alSourcePlay failed, error=0x%X\n",
            error);
        return false;
    }

    return true;
}
```

### 9.1 最关键的修改

```cpp
alSourceStop(source);
```

它的作用是：

```text
如果上一个按键音仍在播放
    -> 立即停止上一个按键音
    -> 再更换 buffer
    -> 从头播放本次按键音
```

对于按键提示音，新的按键打断旧的按键音通常是合理的行为。用户快速点击时，不需要让所有声音排队播放。

### 9.2 为什么要检查 fname

```cpp
if (fname == NULL || fname[0] == '\0')
```

用于防止传入：

```text
空指针
空字符串
```

否则后续 `fopen(fname, "rb")` 可能失败，甚至造成异常。

### 9.3 为什么先清除旧错误

```cpp
while (alGetError() != AL_NO_ERROR)
{
}
```

OpenAL 使用类似“错误状态”的机制。

如果之前某个调用已经产生错误，而代码没有读取，后面调用 `alGetError()` 时可能读到的是旧错误。先清除旧错误，才能更准确地判断当前步骤是否失败。

### 9.4 为什么每一步都调用 alGetError()

例如：

```cpp
alSourceStop(source);
ALenum error = alGetError();
```

这样能明确知道是哪一个操作失败：

```text
停止失败
解绑失败
加载失败
绑定失败
播放失败
```

Visual Studio 的“输出”窗口会显示对应 `TRACE` 信息。

---

## 10. 第二处修改：修正 load() 的错误返回

仍然在：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

找到 `audio::load()` 中的代码：

```cpp
alBufferData(buffer, AL_FORMAT_MONO16, data, data_size, sample_rate);
if (int errcode = alGetError())
{
    printf("Error loading\n");
}
free(data);
fclose(fp);
return 1;
```

这段代码的问题是：

```text
即使 alBufferData() 失败
    -> 只打印 Error loading
    -> 最后仍然 return 1
    -> 上层误以为加载成功
```

将它替换为：

```cpp
alBufferData(
    buffer,
    AL_FORMAT_MONO16,
    data,
    data_size,
    sample_rate);

ALenum error = alGetError();

free(data);
fclose(fp);

if (error != AL_NO_ERROR)
{
    TRACE(
        "audio::load: alBufferData failed, error=0x%X, file=%s\n",
        error,
        fname);

    return false;
}

return true;
```

### 10.1 为什么先保存错误，再释放内存

```cpp
ALenum error = alGetError();
```

先把 OpenAL 错误保存下来，再执行清理代码。

清理工作无论成功还是失败都要执行：

```cpp
free(data);
fclose(fp);
```

这样可以避免：

```text
内存泄漏
文件句柄泄漏
```

### 10.2 为什么加载失败必须 return false

上层代码是：

```cpp
if (!load(fname))
{
    return false;
}
```

所以 `load()` 必须准确报告结果。

```text
加载成功 -> return true
加载失败 -> return false
```

---

## 11. 修改后完整调用过程

修改以后，快速连续输入时会执行：

```text
软键盘按下字符
    -> OnCopyData() 收到 dwData == 2
    -> 构造 WM_KEYDOWN
    -> PreTranslateMessage()
    -> 计算 beepXX.wav 路径
    -> myaudio.play()
    -> audio::play()
    -> 停止上一次按键音
    -> 解绑旧 buffer
    -> 加载 WAV 文件
    -> 绑定 buffer
    -> 播放本次按键音
```

即使用户输入非常快，新按键也会停止旧声音并立即播放新的按键音，不会在播放状态中强行更换 buffer。

---

## 12. 编译方法

在 Visual Studio 中：

```text
1. 打开 D:\workspace\sonoquest_oem\sonoquest.sln
2. 在顶部配置中选择 Debug
3. 平台选择 x64
4. 在解决方案资源管理器中找到 sonoquest 项目
5. 右键 sonoquest
6. 选择“生成”或“重新生成”
```

如果只修改了 `st_audio.cpp`，通常先选择“生成”即可。

如果出现编译状态异常，再执行“重新生成”。

---

## 13. 查看调试日志

在 Visual Studio 中打开：

```text
视图
    -> 输出
```

输出来源选择：

```text
调试
```

如果发生错误，可能看到：

```text
audio::play: alSourceStop failed, error=0xA004
audio::play: detach buffer failed, error=0xA004
audio::load: alBufferData failed, error=0xA004
audio::play: attach buffer failed, error=0xA004
audio::play: alSourcePlay failed, error=0xA004
```

其中：

```text
0xA004 = AL_INVALID_OPERATION
```

日志能帮助判断问题到底发生在哪一步。

---

## 14. 修改后的验收测试

不要只输入两三个字符就认为修复完成。至少进行以下测试。

### 14.1 基本输入

```text
慢速输入 20 个字母
慢速输入 20 个数字
输入空格
输入退格
```

预期：每次操作都能正常输入，并有正确按键音。

### 14.2 快速输入

```text
快速连续输入至少 100 个字符
快速连续点击退格键
快速交替点击两个字符
```

预期：按键音不会永久消失。

快速输入时，前一个声音被后一个声音打断是允许的；关键是后续按键仍然能够持续发声。

### 14.3 长按测试

```text
长按一个字符
长按退格键
```

预期：程序不崩溃，声音模块不会永久失效。

### 14.4 软键盘生命周期测试

```text
打开软键盘
输入字符
关闭或隐藏软键盘
重新打开软键盘
再次输入字符
```

预期：重新打开后按键音仍然正常。

### 14.5 音量测试

测试触控音量：

```text
0
3
9
```

预期：

```text
音量 0 按产品定义静音或播放 beep0.wav
音量 3 使用对应音量文件
音量 9 使用对应音量文件
切换音量后仍能连续播放
```

### 14.6 输入设备对照

分别测试：

```text
软键盘输入
物理键盘输入
```

由于两者最终可能都会进入 `PreTranslateMessage()`，需要确认修改没有影响物理键盘行为。

---

## 15. 修复完成的判断标准

同时满足以下条件，才可以认为修复有效：

```text
1. 软键盘字符输入正常
2. 慢速输入时按键音正常
3. 快速连续输入 100 个字符后，按键音不会永久消失
4. 长按按键不会导致声音模块失效
5. 隐藏并重新打开软键盘后仍有声音
6. 不同触控音量下均无异常
7. Visual Studio 输出窗口没有持续出现 OpenAL 错误
8. 程序没有新增崩溃或卡顿
```

---

## 16. 本次建议修改的文件

本次最小修改范围只有：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

主要修改两个函数：

```cpp
bool audio::play(const char* fname);
bool audio::load(const char* fname);
```

第一轮不要同时修改：

```text
sonoquestDlg.cpp
OnScreenKeyboard.exe
系统音量代码
其他图像或线程模块
```

先用最小范围验证 OpenAL 播放状态问题。这样即使修改没有解决，也容易撤销和继续定位。

---

## 17. 最终排查流程总结

```text
确认现象
    -> 对比慢速输入和快速输入
    -> 找到 OnCopyData() 消息入口
    -> 找到 PreTranslateMessage()
    -> 找到 myaudio.play()
    -> 找到 st_audio.cpp 的 audio::play()
    -> 设置三个断点
    -> 确认声音消失后调用链是否仍然执行
    -> 如果三个断点都进入，定位到 OpenAL 播放模块
    -> 在更换 buffer 前调用 alSourceStop()
    -> 修正 load() 的错误返回
    -> Debug x64 编译
    -> 快速输入 100 个字符验证
```
