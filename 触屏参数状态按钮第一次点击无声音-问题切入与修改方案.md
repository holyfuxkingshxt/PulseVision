# 触屏参数/状态按钮第一次点击没有声音：问题切入与修改方案

## 1. 先把问题说清楚

当前现象是：

> 触屏上有些“参数变化”或者“状态变化”的按钮，第一次点击没有按键音。

这里要先注意一个非常基础但很重要的点：

这个问题不一定走 `CSonoquestDlg::PreTranslateMessage(MSG* pMsg)`。

`PreTranslateMessage` 主要处理 Windows 消息里的键盘消息，例如：

```cpp
WM_KEYDOWN
WM_KEYUP
```

但是触屏按钮很多时候不是键盘输入，而是触屏 HTML 页面先处理点击，再把命令发给主程序。

所以这次排查不能只盯着 `PreTranslateMessage`，应该先看触屏 HTML/JS 的点击链路。

---

## 2. 这类触屏按钮的大致链路

以触屏上一个参数按钮为例，链路大概是：

```text
C:\sonoquest\html\index.html
    ↓
C:\sonoquest\html\main.js / main_add.js
    ↓
JS 处理 click / touchstart / pointerdown
    ↓
console.log("按钮名#参数*touch")
    ↓
TouchScrDlg 触屏程序捕获这个命令
    ↓
WM_COPYDATA 发给 Sonoquest 主程序
    ↓
CSonoquestDlg::OnCopyData()
    ↓
OnMessageTouchCmd()
    ↓
touchscrCmdProc.CmdProc()
    ↓
lq_touchscr::CmdProc()
    ↓
具体 Touch_xxx / Tune_xxx 业务函数
```

也就是说：

```text
声音播放点
```

很可能发生在触屏 HTML/JS 这一层，而不是主程序 C++ 这一层。

---

## 3. 第一个要看的文件：C:\sonoquest\html\main.js

先看这个文件：

```text
C:\sonoquest\html\main.js
```

重点看开头附近：

```js
var audio = new Audio('beep.wav');
```

这说明触屏页面自己创建了一个音频对象，用来播放按键音。

这里的 `beep.wav` 在目录：

```text
C:\sonoquest\html\beep.wav
```

这个文件不是随便来的，主程序系统设置里会把对应音量的 `beepXX.wav` 复制到触屏 HTML 目录。

相关 C++ 文件是：

```text
D:\workspace\sonoquest_oem\sonoquest_src\SysConfigGeneral.cpp
```

里面大概有这样的逻辑：

```cpp
string touchbeep = string(st_dir.get_htmlpage_dir()) + "beep.wav";
CopyFile(oss.str().c_str(), touchbeep.c_str(), 0);
touchscrCmdProc.ApplyVolumeChange();
```

这说明触屏声音本来就是 HTML 页面播放的。

---

## 4. 第二个要看的文件：C:\sonoquest\html\main.js 中按钮声音逻辑

在 `main.js` 中，有一些按钮点击时会直接播放声音，例如：

```js
if (!e.isTrigger) {
  audio.play();
}
```

这里的意思是：

- `e.isTrigger == true`：代码主动触发的点击，不是真人手点。
- `e.isTrigger == false`：用户真实点击触屏。
- 只有用户真实点击时，才播放按键音。

这个判断是合理的，因为程序内部自动更新按钮状态时，不应该一直响。

但是问题就在于：不是所有按钮都走同一套事件。

---

## 5. 第三个要看的文件：C:\sonoquest\html\main_add.js

这次最关键的文件是：

```text
C:\sonoquest\html\main_add.js
```

重点看这几个函数：

```js
function send_btn(cmd) {
  console.log(cmd + '#0' + '*touch');
}

function send_btn1(cmd) {
  console.log(cmd + '#1' + '*touch');
}

function mousedown(event) {
  ...
  send_btn(x);
}

function mousedown1(event) {
  ...
  send_btn1(x);
}
```

这里要理解：

- `.btn` 通常代表参数减小按钮。
- `.btn1` 通常代表参数增大按钮。
- `send_btn(x)` 会发送 `按钮名#0*touch`。
- `send_btn1(x)` 会发送 `按钮名#1*touch`。

也就是说，很多参数变化按钮真正发送命令的位置在：

```text
main_add.js 的 mousedown / mousedown1
```

继续往下看，还会看到绑定事件：

```js
$('.btn').on('pointerdown', mousedown);
$('.btn').on('pointerup pointercancel', mouseup);

$('.btn1').on('pointerdown', mousedown1);
$('.btn1').on('pointerup pointercancel', mouseup);
```

这里说明：

用户按下参数按钮时，真正先执行的是 `pointerdown`，然后进入 `mousedown()` 或 `mousedown1()`。

---

## 6. 为什么第一次点击可能没有声音

从目前代码看，`main.js` 里曾经有 `.btn` / `.btn1` 的 `touchstart` 声音逻辑：

```js
$('.btn').on('touchstart', function (e) {
  if (!e.isTrigger)
    audio.play();
});

$('.btn1').on('touchstart', function (e) {
  if (!e.isTrigger)
    audio.play();
});
```

但是 `main_add.js` 后面又对 `.btn` / `.btn1` 绑定了新的 `pointerdown` 逻辑：

```js
$('.btn').on('pointerdown', mousedown);
$('.btn1').on('pointerdown', mousedown1);
```

而真正发送参数命令的 `mousedown()` / `mousedown1()` 里面没有看到明确的：

```js
audio.play();
```

所以很容易出现这种情况：

```text
第一次按下参数按钮
    ↓
走 pointerdown
    ↓
参数命令发出去了
    ↓
但是没有在 pointerdown 链路里播放声音
    ↓
表现为第一次点击没有按键音
```

对于“状态变化按钮”，如果它也是通过类似的 `pointerdown` 或特殊 click 分支发送命令，也可能出现同样问题。

---

## 7. 新手应该怎么验证

不要急着修改，先做验证。

### 第一步：确认按钮类型

在 `C:\sonoquest\html\index.html` 里搜索按钮 ID。

例如参数按钮一般长这样：

```html
<button class="btn" id="FocusSpacing"></button>
<button class="btn1" id="FocusSpacing"></button>
```

如果按钮 class 是：

```text
btn
btn1
```

那它大概率走 `main_add.js` 的：

```js
mousedown(event)
mousedown1(event)
```

### 第二步：在 main_add.js 里找发送命令的位置

看这两个函数：

```js
function send_btn(cmd)
function send_btn1(cmd)
```

如果点按钮后主程序业务生效，说明命令发送没问题。

问题就是声音没有在这个链路上播放。

### 第三步：确认不是 C++ 键盘音问题

C++ 里的键盘音在：

```text
D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp
```

函数：

```cpp
BOOL CSonoquestDlg::PreTranslateMessage(MSG* pMsg)
```

里面会处理：

```cpp
WM_KEYDOWN
```

但触屏参数按钮不是普通键盘 `WM_KEYDOWN`，所以不要优先改这里。

---

## 8. 建议修改方案

建议在触屏前端加一个统一播放函数，不要到处直接写 `audio.play()`。

建议加在：

```text
C:\sonoquest\html\main.js
```

`var audio = new Audio('beep.wav');` 后面。

例如：

```js
function playTouchBeep() {
  try {
    audio.pause();
    audio.currentTime = 0;
    audio.play();
  } catch (e) {
  }
}
```

为什么要这样写？

- `audio.pause()`：先停掉上一次可能还没播放完的声音。
- `audio.currentTime = 0`：把声音进度拉回开头。
- `audio.play()`：重新播放。
- `try/catch`：避免某些浏览器/CEF 播放失败时报错影响按钮功能。

---

## 9. 修改 mousedown

在：

```text
C:\sonoquest\html\main_add.js
```

找到：

```js
function mousedown(event) {
  if (mousedownID == -1) {
    var x = $(this)[0].id;
    ...
  }
}
```

建议改成：

```js
function mousedown(event) {
  if (mousedownID == -1) {
    playTouchBeep();

    var x = $(this)[0].id;
    var time_interval = 300;
    if (x == 'DynamicalRange' || x == 'FocusPosition') {
      time_interval = 600;
    }
    if (x == 'FreqChange' || x == 'Application') {
      send_btn(x);
      return;
    }

    send_btn(x);
    mousedownID = setInterval(function() { send_btn(x); }, time_interval);
  }
}
```

---

## 10. 修改 mousedown1

同一个文件里找到：

```js
function mousedown1(event) {
  if (mousedownID == -1) {
    var x = $(this)[0].id;
    ...
  }
}
```

建议改成：

```js
function mousedown1(event) {
  if (mousedownID == -1) {
    playTouchBeep();

    var x = $(this)[0].id;
    var time_interval = 300;
    if (x == 'DynamicalRange' || x == 'FocusPosition') {
      time_interval = 600;
    }
    if (x == 'FreqChange' || x == 'Application') {
      send_btn1(x);
      return;
    }

    send_btn1(x);
    mousedownID = setInterval(function() { send_btn1(x); }, time_interval);
  }
}
```

这样做的效果是：

```text
第一次 pointerdown
    ↓
立即 playTouchBeep()
    ↓
再发送参数命令
```

用户感觉就是：第一次点击也有声音。

---

## 11. 为什么不建议优先在 C++ 里加声音

也可以在 C++ 的触屏命令入口加声音，比如：

```text
CSonoquestDlg::OnCopyData()
```

或者：

```text
CSonoquestDlg::OnMessageTouchCmd()
```

但是不建议作为第一选择。

原因是：

1. 触屏 HTML 已经有自己的 `audio.play()`。
2. C++ 再播放一次，可能导致有些按钮响两次。
3. 有些触屏命令是程序自动触发，不是用户点击，C++ 层不容易区分。
4. 前端已经通过 `e.isTrigger` 判断了“用户真实点击”和“程序触发点击”。

所以更合适的修复点是：

```text
C:\sonoquest\html\main_add.js
```

---

## 12. 如果修改后还有问题，再看 C++ 音频层

如果加了 `playTouchBeep()` 后，仍然出现连续点击后声音消失，再回头看 C++ 音频问题。

相关文件：

```text
D:\workspace\sonoquest_oem\sonoquest_src\st_audio.cpp
```

重点函数：

```cpp
bool audio::play(const char* fname)
```

当前逻辑大概是：

```cpp
alSourcei(source, AL_BUFFER, 0);
if (load(fname)) {
    alSourcei(source, AL_BUFFER, buffer);
    alSourcePlay(source);
    return 1;
}
return 0;
```

如果声音还没播放完，又重新 detach buffer，OpenAL 可能出错。

那种问题更像：

```text
连续快速点击后声音消失
```

而这次的描述是：

```text
第一次点击没有声音
```

所以优先看 HTML/JS 更准确。

---

## 13. 建议阅读顺序

### 1. 先看 C:\sonoquest\html\index.html

目的：确认按钮的 `id` 和 `class`。

重点看：

```html
<button class="btn" id="xxx">
<button class="btn1" id="xxx">
<button class="FuncButton ..." id="xxx">
<button class="ModeButton ..." id="xxx">
```

因为不同 class 走不同事件处理。

### 2. 再看 C:\sonoquest\html\main.js

目的：理解触屏声音对象和通用按钮点击逻辑。

重点看：

```js
var audio = new Audio('beep.wav');
```

以及各种：

```js
audio.play();
```

### 3. 再看 C:\sonoquest\html\main_add.js

目的：看参数按钮真正发送命令的位置。

重点看：

```js
function send_btn(cmd)
function send_btn1(cmd)
function mousedown(event)
function mousedown1(event)
```

### 4. 最后看 D:\workspace\sonoquest_oem\sonoquest_src\sonoquestDlg.cpp

目的：理解触屏命令进入主程序后的 C++ 链路。

重点看：

```cpp
BOOL CSonoquestDlg::OnCopyData(CWnd* pWnd, COPYDATASTRUCT* pCopyDataStruct)
LRESULT CSonoquestDlg::OnMessageTouchCmd(WPARAM wparam, LPARAM lparam)
```

### 5. 再看 D:\workspace\sonoquest_oem\sonoquest_src\lq_touchscr.cpp

目的：理解触屏命令如何分发到具体业务函数。

重点看：

```cpp
void lq_touchscr::CmdProc(const string& touchCmdStr)
```

---

## 14. 最终结论

这次问题的优先判断是：

```text
触屏参数/状态按钮第一次点击无声音，优先怀疑触屏 HTML/JS 点击事件链路没有统一播放 beep.wav。
```

最关键的修改点是：

```text
C:\sonoquest\html\main.js
C:\sonoquest\html\main_add.js
```

尤其是：

```js
function mousedown(event)
function mousedown1(event)
```

建议做法：

```text
新增 playTouchBeep()
在 mousedown / mousedown1 第一次进入时调用 playTouchBeep()
```

不要一开始就改 `PreTranslateMessage`，因为触屏参数按钮不是普通键盘输入。
